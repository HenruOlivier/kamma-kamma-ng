import { Routes } from '@angular/router';
import { ContentOutletComponent } from './content/content-outlet/content-outlet.component';

export const routes: Routes = [
    { path: '', component: ContentOutletComponent }
];
